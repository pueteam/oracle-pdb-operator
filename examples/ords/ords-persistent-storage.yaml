apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: oracle-db-ords
  namespace: oracle
spec:
  # storageClassName: oci
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi